<UserControl x:Class="Test.Views.ThreadListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:Test.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="1*" />
		</Grid.RowDefinitions>
		<StackPanel>
			<Button x:Name="AddThread" Content="Add Thread" />
		</StackPanel>
		<TextBlock Grid.Row="1">
			<Run Text="Total Threads:" />
			<Run Text="{Binding Path=Items.Count, Mode=OneWay}" />
		</TextBlock>
		<ListBox ItemsSource="{Binding Items}" HorizontalContentAlignment="Stretch" Grid.Row="2">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<local:ThreadWorkerView DataContext="{Binding}" />
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
    </Grid>
</UserControl>
